import {StyleSheet} from "react-native";
import {COLORS, FONTS} from "../../constants";

export const NFTTitleStyles = StyleSheet.create({
    nftTitleText: {
        fontFamily: FONTS.semiBold,
        color: COLORS.primary
    },
    nftTitleText2: {
        fontFamily: FONTS.regular,
        color: COLORS.primary
    }
});
